{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug Backend & Launch Frontend (via start.py)",
            "type": "python",
            "request": "launch",
            "program": "${workspaceFolder}/start.py",
            "cwd": "${workspaceFolder}",
            "console": "integratedTerminal",
            "justMyCode": true
        }
    ]
}